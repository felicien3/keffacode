services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: keffacode
    ports:
      - "5432:5432"
    volumes:
      - keffacode_data:/var/lib/postgresql/data

volumes:
  keffacode_data:
